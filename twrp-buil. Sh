#!bin/bash

# Go to the working directory
mkdir ~/FOX && cd ~/FOX

# Configure git
git config --global user.email "100Daisy@protonmail.com"
git config --global user.name "GitDaisy"
git config --global color.ui false

# Sync the source
repo init --repo init -u https://gitlab.com/OrangeFox/Manifest.git -b fox_9.0
repo sync  -f --force-sync --no-clone-bundle --no-tags -j$(nproc --all)

# Clone device tree and common tree
git clone --https://github.com/honluciano/omni_device_xiaomi_merlin.git

# Build recovery image
export ALLOW_MISSING_DEPENDENCIES=true; . build/envsetup.sh; lunch omni_merlin-eng; make -j$(nproc --all) recoveryimage
# Rename and copy the files
twrp_version=$(cat ~/FOX/bootable/recovery/variables.h | grep "define TW_MAIN_VERSION_STR" | cut -d '"' -f2)
date_time=$(date +"%d%m%Y%H%M")
mkdir ~/final
cp out/target/product/merlin/recovery.img ~/final/twrp-$twrp_version-"$date_time"-unofficial.img
# Upload to oshi.at
curl -T ~/final/*.img https://oshi.at 
